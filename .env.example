# Server Configuration
PORT=3000
HOST=0.0.0.0
LOG_LEVEL=info

# CORS Configuration
CORS_ORIGIN=*

# Note: LLM and embedding configurations are defined in llm.json
# See README.md for details on configuring models and providers
