services:
  agent-orcha:
    build:
      dockerfile: Dockerfile.amd
      args:
        AMDGPU_TARGETS: ${AMDGPU_TARGETS:-gfx906;gfx908;gfx90a;gfx1030;gfx1100;gfx1101}
    devices:
      - /dev/kfd:/dev/kfd
      - /dev/dri:/dev/dri
    group_add:
      - video
      - render
